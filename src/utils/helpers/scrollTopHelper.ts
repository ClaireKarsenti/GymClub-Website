export const scrollTop = () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth',
  });
};

export const scrollMedium = () => {
  window.scrollTo({
    top: 300,
    behavior: 'smooth',
  });
};
